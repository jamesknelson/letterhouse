---
export interface Props {
  class: string
}

const { class: className = '' } = Astro.props as Props
---

<div class={`Text block ${className}`}>
  <slot />
</div>

<style is:global>
  .Text.block {
    position: relative;

    & * {
      word-wrap: break-word;
      font-family: Karla;
    }

    & blockquote {
      position: relative;
      color: rgba(var(--color-on-surface), var(--opacity-alt));
      margin-top: 1.5rem;
      margin-bottom: 1.5rem;
      padding-left: 2rem;

      &::before {
        content: '“';
        position: absolute;
        left: 0;
        height: 100%;
        width: 1rem;
        font-size: 2rem;
        text-align: center;
        padding-top: 0.5rem;
        box-sizing: border-box;
      }

      & > p {
        margin-left: 0 !important;
      }
    }

    /* blurb quote */
    & h1 + blockquote,
    & blockquote:first-child {
      color: var(--color-primary);
      font-weight: 600;
      opacity: 0.9;
      padding-left: 0;
      margin-top: -0.5rem;
      margin-bottom: 1.5rem;

      &::before {
        display: none;
      }

      & > p {
        margin: 0;
        white-space: pre-line;
        font-size: 1.125rem;
      }
    }

    & h1,
    & h2,
    & h3,
    & h4,
    & h5,
    & h6 {
      &,
      * {
        font-family: Karma;
      }
    }

    & strong {
      font-weight: bold;
    }

    & em {
      font-style: italic;
    }

    & code,
    & pre {
      /* background-color: var(--color-alt-surface) !important; */
    }

    & code {
      /* color: var(--color-on-surface); */
      font-family: monospace;
      font-size: 95%;
      padding: 0.15em;
    }

    & ol,
    & ul {
      padding-left: 30px;
    }

    & p,
    & h1,
    & h2,
    & h3,
    & h4,
    & h5,
    & h6,
    & ul,
    & ol,
    & img,
    & pre,
    & hr,
    & blockquote {
      font-size: 1rem;
      line-height: 1.5rem;
      margin: 1rem 0;
    }

    & hr {
      left: 33% !important;
      margin: 1.5rem 1rem;
      position: relative;
      width: 33% !important;
      height: 1px;
      background-color: var(--color-surface-line);
    }

    & h1,
    & h1 a,
    & h2,
    & h2 a {
      color: var(--color-primary);
      font-family: Karma;
    }

    & h1 {
      font-size: 2rem;
      line-height: 2rem;
      font-weight: 600;
      margin-top: 2rem;
    }

    & h2 {
      line-height: 1.5rem;
      font-weight: 600;
      font-size: 1.5rem;
      margin-top: 3rem;
    }

    & h3 {
      font-size: 22px;
      line-height: 2rem;
      font-weight: 600;
    }

    & h4,
    & h5,
    & h6 {
      font-size: 20px;
      line-height: 1.5rem;
      font-weight: 600;
      margin-top: 1rem;
    }

    & p a,
    & li a {
      color: var(--color-primary);
      text-decoration: underline;

      &:hover {
        color: var(--color-secondary);
      }
    }
  }
</style>
